<Page
    x:Class="Anchor.Pages.DrivesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="32,24">
        <StackPanel MaxWidth="900">
            <TextBlock Style="{StaticResource PageTitle}" Text="Drive Health" />
            <TextBlock Style="{StaticResource CardCaption}" Margin="0,-12,0,16"
                       Text="Early warnings for SSD/HDD failure using Windows' own S.M.A.R.T. and storage-reliability data. If you ever see a warning here, back up first, diagnose second." />

            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                <Button Content="Scan drives" Click="Scan_Click" Style="{StaticResource AccentButtonStyle}" />
                <ProgressRing x:Name="Progress" IsActive="False" Width="24" Height="24" />
            </StackPanel>

            <ItemsControl x:Name="DrivesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}">
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Style="{StaticResource CardBorder}">
                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource CardHeader}" Text="Disk space" />
                    <ItemsControl x:Name="SpaceList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="0,2" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
