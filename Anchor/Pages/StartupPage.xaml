<Page
    x:Class="Anchor.Pages.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:Anchor.Services">

    <Grid Padding="32,24" MaxWidth="960">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource PageTitle}" Text="Startup Apps" />
            <TextBlock Style="{StaticResource CardCaption}" Margin="0,-12,0,12"
                       Text="Apps that launch when Windows boots. Turning one off doesn't uninstall it — it just stops it auto-starting. Flip the toggle back any time. (Same mechanism as Task Manager's Startup tab.)" />
            <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,0,12" />
        </StackPanel>

        <ListView Grid.Row="1" x:Name="EntriesList" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="svc:StartupEntry">
                    <Border Style="{StaticResource CardBorder}" Margin="0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="2">
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                <TextBlock Style="{StaticResource CardCaption}" Text="{x:Bind Command}" />
                                <TextBlock Style="{StaticResource CardCaption}" Text="{x:Bind Scope}" />
                            </StackPanel>
                            <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Enabled}" Toggled="Entry_Toggled"
                                          Tag="{x:Bind}" OnContent="On" OffContent="Off" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
